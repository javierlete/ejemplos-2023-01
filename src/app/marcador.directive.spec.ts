import { MarcadorDirective } from './marcador.directive';

describe('MarcadorDirective', () => {
  it('should create an instance', () => {
    const directive = new MarcadorDirective();
    expect(directive).toBeTruthy();
  });
});
